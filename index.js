import { LRParser } from '@lezer/lr';
import { LRLanguage, indentNodeProp, delimitedIndent, foldNodeProp, foldInside, syntaxTree, LanguageSupport } from '@codemirror/language';
import { styleTags, tags } from '@lezer/highlight';
import { IterMode } from '@lezer/common';

// This file was generated by lezer-generator. You probably shouldn't edit it.
const parser = LRParser.deserialize({
  version: 14,
  states: "%jQYQPOOO_QPO'#C_OOQO'#Ci'#CiQYQPOOOdQQO'#CaOOQO'#Cj'#CjOrQPO,58yOOQO-E6g-E6gOOQO'#Cb'#CbO}QPO,58{OOQO-E6h-E6hO!SQSO1G.gO!bQPO'#CgOOQO'#Cf'#CfO!gQSO'#CfOdQQO'#CdOOQO'#Cd'#CdO!lQPO7+$RO!qQWO,59RO!yQPO,59QO#RQPO,59OO#WQPO<<GmO#]QPO1G.mO!gQSO'#CkO#bQPO1G.lOOQO1G.l1G.lO#jQ`O1G.jOOQOAN=XAN=XOOQO7+$X7+$XOOQO,59V,59VOOQO-E6i-E6iOOQO7+$W7+$WOOQO'#Ce'#CeO#rQPO7+$UOOQO<<Gp<<Gp",
  stateData: "#w~ObOSPOS~OSPO~OcSO~OVWOdWOeWOfWO~OcSOSRa`Ra~OgZO~O[[Oh_On]Oo^O~OhbO~O[[O~OieO~OVfOmfO~OigOpiO~OijO~OSkO~OllO~OigOpoO~OjpOkpO~OlrO~O",
  goto: "!e`PPPaPeiPoruxP!R!X!_TQORTTPUQXSRd_RaZRqjR`ZQ]ZQc^RmgQRORVRQUPRYUQhcRnh",
  nodeNames: "⚠ Comment File State Sname State_rule Tape_symbol Var_name Tape_action Head_move Fun_sequence Function Fun_name",
  maxTerm: 32,
  skippedNodes: [0,1],
  repeatNodeCount: 3,
  tokenData: "(s~RjXY!sYZ!s]^!spq!sxy#Uyz#Z|}#`}!O#e!P!Q#p!Q!R$_!R!S$d!S![$k!c!n$p!n!o&`!o!t$p!t!u&w!u!}$p!}#O'`#P#Q'm#R#S'r#T#o%{#p#q(V$Jb'&l([;(b;(c(a;(c;(d(g;(d;(e(m~!xSb~XY!sYZ!s]^!spq!s~#ZOh~~#`Ol~~#eOi~~#hP!`!a#k~#pOg~~#sP!P!Q#v~#{SP~OY#vZ;'S#v;'S;=`$X<%lO#v~$[P;=`<%l#v~$dOd~Y$kOeQmWW$pOmW_$yS[SSPVY!R![$p!c!}$p#R#S%V#T#o%{T%^S[SSP!R![%V!c!}%V#R#S%V#T#o%jP%oSSP!R![%j!c!}%j#R#S%j#T#o%jZ&SSSPVY!R![%{!c!}%{#R#S%j#T#o%{o&kSj`[SSPVY!R![$p!c!}$p#R#S%V#T#o%{o'SSk`[SSPVY!R![$p!c!}$p#R#S%V#T#o%{~'ePo~#P#Q'h~'mOn~~'rOp~R'ySfQSP!R![%j!c!}%j#R#S%j#T#o%j~([Oc~P(aOSPP(dP;=d<%l([P(jP;=`<%l([P(pP;=`;My([",
  tokenizers: [0, 1, 2, 3, 4],
  topRules: {"File":[0,2]},
  tokenPrec: 0
});

const TURNIPLang = LRLanguage.define({
    parser: parser.configure({
        props: [
            indentNodeProp.add({
                Application: delimitedIndent({ closing: ")", align: false }),
            }),
            foldNodeProp.add({
                Application: foldInside,
            }),
            styleTags({
                Tape_symbol: tags.literal,
                Head_move: tags.typeName,
                //Tape_action: t.
                //State_rule: t.
                //State: t.
                Sname: tags.variableName,
                Comment: tags.comment,
                "->": tags.operator,
                "|": tags.operator,
                "( )": tags.paren,
                ",": tags.operator,
            }),
        ],
    }),
    languageData: {
        commentTokens: { line: "#" },
    },
});
// TODO: we may want use cache
// See https://github.com/codemirror/lang-python/blob/main/src/complete.ts#L61
/**
 * Construct a completions list containing all defined states.
 * @param tree
 * @param doc
 * @param excludedFrom position of the current node, we don't want to include the partially written state in the completion list
 * @returns
 */
function getStateNames(tree, doc, excludedFrom) {
    const names = new Set();
    tree.cursor(IterMode.IncludeAnonymous).iterate((node) => {
        if (node.type.is("Sname") && node.from !== excludedFrom) {
            names.add(doc.sliceString(node.from, node.to));
        }
    });
    let completions = [
        { label: "START", type: "constant" },
        { label: "END", type: "constant" },
    ];
    for (const name of names) {
        if (name !== "START" && name !== "END") {
            completions.push({ label: name, type: "variable" });
        }
    }
    return completions;
}
function turnipCompletionFor(context) {
    let { state, pos } = context;
    let tree = syntaxTree(state).resolveInner(pos, -1);
    //let around = tree.resolve(pos)
    // tree.from correspond to the start of the current node
    const stateNames = getStateNames(syntaxTree(state), state.doc, tree.from);
    // TODO: we may want to propose Sname autocomplete even when a Sname is not started, but only expected
    if (tree.name == "Sname") {
        return {
            from: tree.from,
            options: stateNames,
        };
    }
    // TODO: we may want to autocomplete complete states
}
const turnipCompletion = TURNIPLang.data.of({
    autocomplete: turnipCompletionFor,
});
function turnip_lang() {
    return new LanguageSupport(TURNIPLang, [turnipCompletion]);
}

export { TURNIPLang, turnipCompletion, turnip_lang };
